---
inclusion: auto
name: quick-reference
description: Quick reference for gRPC templates and common patterns. Use when writing gRPC handlers, services, or looking up common code snippets.
---

# Quick Reference - gRPC шаблоны

> Команды: `tech.md` | Тестовые пользователи: `lessons-learned.md` | Workflow: `development-workflow.md`

## 🚀 Новый gRPC метод

```go
func (s *API) NewMethod(ctx context.Context, req *pb.Request) (*pb.Response, error) {
    if err := s.validateServer(); err != nil {
        return nil, err
    }
    if err := validateRequiredFields(map[string]string{"field": req.GetField()}); err != nil {
        return nil, err
    }
    ctx, cancel, err := withTimeout(ctx, 5*time.Second)
    if err != nil {
        return nil, err
    }
    defer cancel()
    
    result, err := s.service.DoSomething(ctx, req.GetField())
    if err != nil {
        s.log.Error("failed", slog.String("error", err.Error()))
        return nil, status.Error(codes.Internal, "operation failed")
    }
    return &pb.Response{Result: result}, nil
}
```

**Interceptor уже проверяет:** `ctx != nil`, `req != nil`

## 📁 Добавление нового метода

### 1. Proto wrapper (КРИТИЧНО!)
```typescript
// shared/api/generated/user/user_pb_wrapper.ts
if (!proto.NewMethodRequest) {
  throw new Error('Proto class NewMethodRequest not found');
}
if (!proto.NewMethodResponse) {
  throw new Error('Proto class NewMethodResponse not found');
}
export const NewMethodRequest = proto.NewMethodRequest;
export const NewMethodResponse = proto.NewMethodResponse;
```


### 2. gRPC Client
```typescript
// shared/lib/grpc/grpc-client.ts

// Добавить в METHODS:
const METHODS = { NEW_METHOD: 'NewMethod' };

// Добавить в grpcClient:
newMethod: async (request: NewMethodRequestType): Promise<NewMethodResponseType> => {
  const ResponseClass = await getUserResponseClass<NewMethodResponseType>('NewMethodResponse');
  return grpcCall(USER_SERVICE, METHODS.NEW_METHOD, request, ResponseClass);
},
```

### 3. Публичный метод (без auth)
```go
// Backend: internal/lib/interceptors/auth.go
var publicMethods = map[string]bool{
    "/user.UserService/NewMethod": true,
}
```
```typescript
// Frontend: shared/lib/grpc/grpc-client.ts
const PUBLIC_METHODS: AllowedMethod[] = [
  METHODS.NEW_METHOD,  // Добавить если публичный
];
```

**Полный чеклист:** `#grpc-workflow`

## 🎯 Частые ошибки

| Симптом | Причина |
|---------|---------|
| `failed to unmarshal` | **Stub файлы вместо сгенерированных proto!** |
| Метод молча не работает | Забыл user_pb_wrapper.ts |
| Error deserializing response | Забыл добавить метод в `grpcClient` с `getXxxResponseClass` |
| Unauthenticated 401 / 0 bytes | Забыл publicMethods (backend) |
| Запросы висят | Нет timeout |
| Import error | Прямой импорт _pb (нужен wrapper) |

### ⚠️ Proto stub vs generated (КРИТИЧНО!)
```bash
# Проверить что proto сгенерированы правильно:
head -5 internal/grpc/gen/user/user.pb.go
# Должно быть: // Code generated by protoc-gen-go. DO NOT EDIT.

# Если нет — перегенерировать:
cd contract && task generate
```

## 🔍 Отладка gRPC

```typescript
// Логирование response
const data = await response.arrayBuffer();
console.log('Response size:', data.byteLength);
```

**Network tab:** POST /service.Service/Method, Content-Type: application/grpc-web+proto

**Пустой ответ (0 bytes):** Метод в publicMethods но требует auth, или ошибка в service не залогирована
